var game = {
	init : function(){
		// TODO:
		// All the code that needs to execute
		// when the game begins.
	},


	generateSequence : function(){
		// TODO:
		// all the code that is required
		// to generate a sequence.
	},

	// Then number to equate both weights
	finalResult : generateSequence().sum(), 

	// weights on the right
	weight1 : [],

	// weights on the left
	weight2 : [],

	total weights to begin with
	sequence : [],

	gameFunction : function(number, balanceType){
		// params : number : the number entered by user,
		// balanceType : where the number was entered (left / right)
		// TODO:
		// everytime a user enters a number in any weight
	},

	winCondition : function(){
		// TODO :
		// Check weather a game is won, lost or needs to continue
		// should return true for win, false for continue.
	}

};